mosh on FreeBSD
===============

1. Install
----------

FreeBSD ports collection has 'lang/mosh' as official (n)mosh distribution.

- Install following ports:
  devel/gmake
  devel/oniguruma5
  math/gmp
- ./configure CFLAGS=-I/usr/local/include LDFLAGS=-L/usr/local/lib
- gmake
- sudo gmake install

mosh cannot build using BSD "make".

2. Limitations
--------------

- mosh is currently only tested at i386 and amd64 architecture.

